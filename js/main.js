define(
    ["jquery", "myModule"],
    function ($, myModule ) {
        // myModule.onNext();
        // myModule.createQuestion();
        // myModule.createRadios();
        myModule.initialize();
    }
);
